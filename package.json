{
  "name": "early-warning-system",
  "version": "2.0.0",
  "description": "AI-Based Early Warning System for Disease Outbreaks - Tamil Nadu",
  "scripts": {
    "dev": "start cmd /k \"cd backend && uvicorn main:app --reload --port 8000\" && cd frontend && npm start",
    "backend": "cd backend && uvicorn main:app --reload --port 8000",
    "frontend": "cd frontend && npm start",
    "train": "python run_pipeline.py",
    "install-all": "cd frontend && npm install"
  }
}
